using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UIManager : MonoBehaviour
{
    //In UIManager, we should be able to change anything relevant about the game interface. (player sprinting dials, screen effects, see player lives, etc.)
    //Using the singletons, we can easily change the master volume, the volume of
    //specific components, the difficulty of the game, or the sensitivity of the camera.
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
